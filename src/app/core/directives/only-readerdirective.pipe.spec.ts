import { OnlyReaderdirectivesPipe } from './only-readerdirective.pipe';

describe('OnlyReaderdirectivesPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyReaderdirectivesPipe();
    expect(pipe).toBeTruthy();
  });
});
